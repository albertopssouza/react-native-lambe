import React from 'react'
import { NavigationContainer } from '@react-navigation/native'

import Tab from './Tab'

export default () => {
    return (
        <NavigationContainer>
            <Tab />
        </NavigationContainer>
    )
}